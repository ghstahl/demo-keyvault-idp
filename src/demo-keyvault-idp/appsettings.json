{
  "KeyVault": {
    "BaseUrl": "https://P7KeyValut.vault.azure.net/"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "Identity": {

    "KeyVault": "in environment settings",
    "KeyIdentifier": "in environment settings",
    "AzureAd": {
      "ClientId": "{{secret}}",
      "ClientSecret": "{{secret}}"
    }
  }
}
